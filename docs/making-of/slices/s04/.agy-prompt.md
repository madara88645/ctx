You are working only in this sandbox: ~/agy-sandbox/ctx-cli

Implement the requested change now. This is a non-interactive run: do NOT ask for
confirmation, do NOT stop at a design proposal, do NOT wait for approval.

First read ~/agy-sandbox/ctx-cli/plan.md for full context (read-only).
Then implement ONLY Slice S4 (fingerprint diff). Do not implement any other slice.

Context from previous slices: src/fingerprint.mjs already exports
snapshotDir(rootAbs) -> { files: Record<string,string>, truncated: boolean }.
test/fingerprint.test.mjs already tests snapshotDir — keep those tests passing.

Goal: add the pure diff function to the existing fingerprint module.

You MUST modify exactly these files:
- ~/agy-sandbox/ctx-cli/src/fingerprint.mjs — ADD (do not change
  snapshotDir): export function
  diffSnapshots(oldFiles, newFiles) -> { added: string[], removed: string[], modified: string[] }
  * added: keys in newFiles but not oldFiles; removed: keys in oldFiles but not newFiles;
    modified: keys in both with different values.
  * All three arrays sorted ascending (plain string comparison).
  * Pure function: no fs, no I/O; treat missing/null args as empty objects.
- ~/agy-sandbox/ctx-cli/test/fingerprint.test.mjs — EXTEND with exact
  assertions:
  * diffSnapshots({}, {"a":"1:1"}) -> {added:["a"], removed:[], modified:[]}
  * diffSnapshots({"a":"1:1"}, {}) -> {added:[], removed:["a"], modified:[]}
  * diffSnapshots({"a":"1:1"}, {"a":"2:1"}) -> {added:[], removed:[], modified:["a"]}
  * mixed case with ≥2 entries per category, asserting full deep-equal of the result
  * diffSnapshots(null, undefined) -> {added:[], removed:[], modified:[]}

Do not touch any other file. Locked read-only: plan.md, prompt.md.
Do not use the network, do not install packages (Node.js built-ins only), do not use git.

Acceptance checks:
1. diffSnapshots exported by that exact name; snapshotDir behavior unchanged.
2. `node --test` passes (all suites green).

Run only this validation command: cd ~/agy-sandbox/ctx-cli && node --test
At the end, state changed files, validation output, and any remaining limitation.
