You are working only in this sandbox: ~/agy-sandbox/ctx-cli

Implement the requested change now. This is a non-interactive run: do NOT ask for
confirmation, do NOT stop at a design proposal, do NOT wait for approval.

First read ~/agy-sandbox/ctx-cli/plan.md for full context (read-only) —
especially the Slice S7 contract and the store schema. Then implement ONLY Slice S7
(commands). Do not implement any other slice (bin wiring is S8 — do not touch bin/ctx.mjs).

Context — these modules exist and are tested; import from them, do not reimplement:
- src/timefmt.mjs: formatRelative(thenMs, nowMs) -> string
- src/fingerprint.mjs: snapshotDir(rootAbs) -> {files: Record<string,string>, truncated: boolean};
  diffSnapshots(oldFiles, newFiles) -> {added: string[], removed: string[], modified: string[]}
- src/store.mjs: storePath() -> string; loadStore() -> store; saveStore(store) -> void
- src/git.mjs: gitInfo(dirAbs) -> {branch, statusShort} | null
Store schema: store.projects[absPath] = { savedAt: epochMs, note, files, truncated, git }.

Goal: the three command composers. Each returns { ok: boolean, text: string } and does NOT
print anything itself. `now` parameter defaults to Date.now().

You MUST create exactly these files:
- ~/agy-sandbox/ctx-cli/src/commands.mjs — exports:
  * cmdSave(cwdAbs, noteWords, now = Date.now()) -> {ok,text}
    - note = noteWords.join(" ").trim(), truncated to at most 500 chars.
    - empty note → { ok: false, text: "Note required. Usage: ctx save <note>" } and NO store write.
    - else: snap = snapshotDir(cwdAbs); store the entry { savedAt: now, note,
      files: snap.files, truncated: snap.truncated, git: gitInfo(cwdAbs) } under key cwdAbs;
      saveStore; return { ok: true, text: `Saved snapshot for ${cwdAbs} (${N} files tracked).` }
      where N = number of keys in snap.files.
  * cmdResume(cwdAbs, now = Date.now()) -> {ok,text}, always ok: true.
    - no entry for cwdAbs → text: `No snapshot for ${cwdAbs} yet. Run: ctx save "<note>"`
    - else build lines joined with "\n":
      1. `Resume brief for ${cwdAbs}`
      2. `Last save: ${formatRelative(entry.savedAt, now)}`
      3. `Note: ${entry.note}`
      4. only when entry.git is non-null: `Git then: ${entry.git.branch}`
      then diff = diffSnapshots(entry.files, snapshotDir(cwdAbs).files):
      - all three arrays empty → one more line `No file changes since then.`
      - else for each non-empty category, in this order Added, Modified, Removed, a line
        `Added (N): a, b` — comma+space separated names, at most 20 names, and if more
        than 20 append `, …` after the 20th.
  * cmdList(now = Date.now()) -> {ok,text}, always ok: true.
    - loadStore(); no projects → text: `No snapshots yet. Run: ctx save "<note>" inside a project.`
    - else one line per project sorted by savedAt descending:
      `${formatRelative(savedAt, now)}  ${path}  —  ${note}` with note truncated to 60 chars
      (append `…` when truncated).
- ~/agy-sandbox/ctx-cli/test/commands.test.mjs — node:test + node:assert.
  Setup per test: fresh temp CTX_HOME (restore env after) + a fresh temp project dir with
  1-2 real files; use a FIXED now (e.g. const NOW = 1_752_570_000_000). Cases (exact-string
  asserts on .text, plus .ok):
  * cmdSave with empty noteWords -> ok false, exact usage text, and loadStore() still empty
  * cmdSave with ["fix","auth","bug"] -> ok true, text matches `Saved snapshot for <dir> (2 files tracked).`;
    store entry has note "fix auth bug", savedAt NOW, files with 2 keys
  * note truncation: 600-char note saved as 500 chars
  * cmdResume with no entry -> exact "No snapshot..." text
  * cmdResume after save with no changes -> contains "Last save: just now" line and
    "No file changes since then."
  * cmdResume after adding one file and modifying one (rewrite with different size)
    -> contains `Added (1): ` and `Modified (1): ` lines with the right names
  * 21+ added files -> the Added line ends with `, …`
  * cmdList empty -> exact text; after two saves in two dirs (savedAt NOW and NOW-60_000)
    -> two lines, most recent first, second line starts with "1 minute ago"
  * cmdList 70-char note -> 60 chars + `…`

Do not touch any other file (especially not bin/ctx.mjs). Locked read-only: plan.md, prompt.md.
Do not use the network, do not install packages (Node.js built-ins only), do not use git.

Acceptance checks:
1. cmdSave, cmdResume, cmdList exported by those exact names with the exact texts above.
2. `node --test` passes (all suites green).

Run only this validation command: cd ~/agy-sandbox/ctx-cli && node --test
At the end, state changed files, validation output, and any remaining limitation.
