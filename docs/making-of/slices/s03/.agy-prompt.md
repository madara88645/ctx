You are working only in this sandbox: ~/agy-sandbox/ctx-cli

Implement the requested change now. This is a non-interactive run: do NOT ask for
confirmation, do NOT stop at a design proposal, do NOT wait for approval.

First read ~/agy-sandbox/ctx-cli/plan.md for full context (read-only).
Then implement ONLY Slice S3 (fingerprint walk). Do not implement any other slice
(diffSnapshots comes in a LATER slice — do not add it now).

Context from previous slices: package.json is "type":"module" with test script
"node --test"; src/timefmt.mjs and tests for it exist.

Goal: the directory fingerprint snapshot function.

You MUST create exactly these files:
- ~/agy-sandbox/ctx-cli/src/fingerprint.mjs — export function
  snapshotDir(rootAbs) -> { files: Record<string,string>, truncated: boolean }.
  Behavior:
  * Recursive walk of rootAbs using node:fs (synchronous APIs are fine).
  * Keys are file paths RELATIVE to rootAbs, always with "/" separators (POSIX style).
  * Values are `${stat.size}:${Math.floor(stat.mtimeMs)}`.
  * Skip directories named exactly: node_modules, .git, dist, build, __pycache__ (at any depth).
  * Skip files named exactly: .DS_Store
  * Skip symbolic links entirely (use lstat to detect them).
  * Deterministic: build the result with keys sorted ascending (localeCompare-free,
    plain < comparison on strings).
  * Stop adding entries once 5000 files are recorded; if anything was left out because
    of the cap, set truncated: true, else false.
- ~/agy-sandbox/ctx-cli/test/fingerprint.test.mjs — node:test + node:assert.
  Build a temp tree with fs.mkdtempSync(path.join(os.tmpdir(), "ctx-test-")):
    a.txt, sub/b.txt, node_modules/x.js, .DS_Store, and a nested sub/deep/c.txt.
  Assert: exact sorted key list ["a.txt","sub/b.txt","sub/deep/c.txt"]; each value equals
  the real `${size}:${floor(mtimeMs)}` from fs.statSync; truncated === false.
  Add a second test: empty temp dir -> { files: {}, truncated: false }.

Do not touch any other file (do not modify bin/ctx.mjs, package.json, README.md,
src/timefmt.mjs, or existing tests). Locked read-only: plan.md, prompt.md.
Do not use the network, do not install packages (Node.js built-ins only), do not use git.

Acceptance checks:
1. Both files exist; snapshotDir exported by that exact name.
2. `node --test` passes (all suites green).

Run only this validation command: cd ~/agy-sandbox/ctx-cli && node --test
At the end, state changed files, validation output, and any remaining limitation.
