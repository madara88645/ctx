You are working only in this sandbox: ~/agy-sandbox/ctx-cli

Implement the requested change now. This is a non-interactive run: do NOT ask for
confirmation, do NOT stop at a design proposal, do NOT wait for approval.

First read ~/agy-sandbox/ctx-cli/plan.md for full context (read-only).
Then implement ONLY Slice S5 (store). Do not implement any other slice.

Context from previous slices: package.json is "type":"module", test script
"node --test"; src/timefmt.mjs, src/fingerprint.mjs and their tests exist.

Goal: the JSON store module.

You MUST create exactly these files:
- ~/agy-sandbox/ctx-cli/src/store.mjs — exports:
  * storePath() -> string: dir = process.env.CTX_HOME ?? path.join(os.homedir(), ".ctx");
    return path.join(dir, "store.json").
  * loadStore() -> object: read+parse storePath(); if the file is missing, unreadable,
    invalid JSON, or not an object → return { version: 1, projects: {} }. Never throws.
  * saveStore(store) -> void: mkdir the store dir recursively if needed, then write
    JSON.stringify(store, null, 2) + "\n" to storePath().
- ~/agy-sandbox/ctx-cli/test/store.test.mjs — node:test + node:assert.
  Every test sets process.env.CTX_HOME to a fresh fs.mkdtempSync(path.join(os.tmpdir(),
  "ctx-test-")) dir (and restores the old value after). Cases:
  * storePath() === path.join(CTX_HOME, "store.json")
  * loadStore() with no file -> deep-equal { version: 1, projects: {} }
  * saveStore({version:1, projects:{"/p":{savedAt:1, note:"n", files:{}, truncated:false, git:null}}})
    then loadStore() -> deep-equal roundtrip
  * write the literal text "not json{" to store.json, loadStore() -> { version: 1, projects: {} }

Do not touch any other file. Locked read-only: plan.md, prompt.md.
Do not use the network, do not install packages (Node.js built-ins only), do not use git.

Acceptance checks:
1. storePath, loadStore, saveStore exported by those exact names.
2. `node --test` passes (all suites green).

Run only this validation command: cd ~/agy-sandbox/ctx-cli && node --test
At the end, state changed files, validation output, and any remaining limitation.
