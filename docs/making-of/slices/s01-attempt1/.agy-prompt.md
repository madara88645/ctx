You are working only in this sandbox: ~/agy-sandbox/ctx-cli

Implement the requested change now. This is a non-interactive run: do NOT ask for
confirmation, do NOT stop at a design proposal, do NOT wait for approval.

First read ~/agy-sandbox/ctx-cli/plan.md for full context (read-only).
Then implement ONLY Slice S1 (Scaffold + help contract). Do not implement any other slice.

Goal: scaffold the zero-dependency "ctx" CLI and its help contract.

You MUST create exactly these files:
- ~/agy-sandbox/ctx-cli/package.json — name "ctx-cli", "type":"module",
  "bin":{"ctx":"bin/ctx.mjs"}, "scripts":{"test":"node --test test/"}, engines node >=20.
  NO dependencies and NO devDependencies keys with content.
- ~/agy-sandbox/ctx-cli/bin/ctx.mjs — argv dispatcher. For now: no args or
  "help" → print a help text (multi-line) that contains these three literal lines and exit 0:
    ctx save <note>
    ctx resume
    ctx list
  Any unknown command → print the same help to stdout and exit with code 1.
- ~/agy-sandbox/ctx-cli/README.md — short stub: what ctx is (context
  snapshot & resume CLI), Node >= 20, zero dependencies, "full docs coming in later slices".
- ~/agy-sandbox/ctx-cli/test/cli-help.test.mjs — node:test suite using
  child_process.spawnSync(process.execPath, [<absolute path to bin/ctx.mjs resolved from
  the test file's URL>], ...): (a) no args → exit 0 and stdout contains "ctx save <note>";
  (b) arg "bogus" → exit status 1.

Do not touch any other file. Locked read-only: plan.md, prompt.md.
Do not use the network, do not install packages (Node.js built-ins only), do not use git.

Acceptance checks:
1. The four files above exist with the described behavior.
2. `node --test test/` passes (2 tests green).

Run only this validation command: cd ~/agy-sandbox/ctx-cli && node --test test/
At the end, state changed files, validation output, and any remaining limitation.
