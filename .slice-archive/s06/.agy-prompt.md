You are working only in this sandbox: /Users/mehmetozel/agy-sandbox/ctx-cli

Implement the requested change now. This is a non-interactive run: do NOT ask for
confirmation, do NOT stop at a design proposal, do NOT wait for approval.

First read /Users/mehmetozel/agy-sandbox/ctx-cli/plan.md for full context (read-only).
Then implement ONLY Slice S6 (git probe). Do not implement any other slice.

Context from previous slices: package.json is "type":"module", test script
"node --test"; src/timefmt.mjs, src/fingerprint.mjs, src/store.mjs and tests exist.

Goal: the best-effort git info module. NOTE: the module SHELLS OUT to the git binary at
the USER's runtime; your tests only exercise paths where git yields no repo info. You must
not run any git command yourself outside of executing this module's test suite.

You MUST create exactly these files:
- /Users/mehmetozel/agy-sandbox/ctx-cli/src/git.mjs — exports:
  * gitInfo(dirAbs) -> { branch: string, statusShort: string } | null
  * Implementation: child_process.spawnSync("git", ["rev-parse","--abbrev-ref","HEAD"],
    { cwd: dirAbs, encoding: "utf8", timeout: 1500 }). If error, non-zero status, or empty
    stdout → return null. Else branch = stdout.trim(). Then spawnSync("git",
    ["status","--short"], same options); if that errors or exits non-zero → return null;
    statusShort = stdout.trimEnd() (may be ""). Wrap EVERYTHING in try/catch; on any throw
    return null. The function must never throw and never print.
- /Users/mehmetozel/agy-sandbox/ctx-cli/test/git.test.mjs — node:test + node:assert:
  * gitInfo on a fresh fs.mkdtempSync temp dir (not a git repo) -> null
  * gitInfo on a path that does not exist (e.g. path.join(tmp, "nope-xyz")) -> null
  * typeof gitInfo === "function" and it returns without throwing when given "/"

Do not touch any other file. Locked read-only: plan.md, prompt.md.
Do not use the network, do not install packages (Node.js built-ins only), and do not run
any git command directly (the module under test may invoke git internally when the tests run).

Acceptance checks:
1. gitInfo exported by that exact name; never throws.
2. `node --test` passes (all suites green).

Run only this validation command: cd /Users/mehmetozel/agy-sandbox/ctx-cli && node --test
At the end, state changed files, validation output, and any remaining limitation.
